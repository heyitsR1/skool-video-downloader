# Build outputs — distributed as GitHub Release assets, not committed.
dist/
*.zip

# Internal design docs / specs (not for the public repo).
docs/

node_modules/
.DS_Store

# Local wrangler state/cache — not source.
.wrangler/

# The licensing Worker is a separate, private service.
worker/

# Maintainer-only release tooling (lives with the Worker).
scripts/publish-version.mjs
